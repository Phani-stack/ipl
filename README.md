navigate to model folder run the command
    pip install pandas numpy scikit-learn xgboost

train the model first 
    python train.py

change the details of based on current match trends in predict.py at line 27 and run
    python predict.py
